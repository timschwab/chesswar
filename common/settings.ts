const serverPort = 18357;
const serverHostname = "0.0.0.0";
const acceleration = 0.5;
const maxSpeed = 40;

const settings = {
	serverPort,
	serverHostname,
	acceleration,
	maxSpeed
};

export default settings;