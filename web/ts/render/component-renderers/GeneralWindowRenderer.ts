import { ChesswarState } from "../../game-logic/ChesswarState.ts";
import { UiComponentRenderer } from "../UiComponentRenderer.ts";

export class GeneralWindowRenderer implements UiComponentRenderer {
    render(_state: ChesswarState) {
        //
    }
}
